# Copy this into your RHDH dynamic-plugins file.
# If testing with RHDH-local, paste this into dynamic-plugins.override.yaml

extensions:
  installation:
    enabled: true
    saveToSingleFile:
       file: dynamic-plugins.override.yaml

includes:
  - dynamic-plugins.default.yaml

plugins:
  # Github Actions
  - package: oci://ghcr.io/redhat-developer/rhdh-plugin-export-overlays/backstage-community-plugin-github-actions:<tag>
    forceDownload: true
    pullPolicy: Always
    disabled: false

  # RBAC
  - package: oci://ghcr.io/redhat-developer/rhdh-plugin-export-overlays/backstage-community-plugin-rbac:<tag>
    forceDownload: true
    pullPolicy: Always
    disabled: false
    pluginConfig:
      dynamicPlugins:
        frontend:
          backstage-community.plugin-rbac:
            translationResources:
              - importName: rbacTranslations
                ref: rbacTranslationRef
                module: Alpha
            appIcons:
              - name: rbacIcon
                importName: RbacIcon
            dynamicRoutes:
              - path: /rbac
                importName: RbacPage
                menuItem:
                  icon: rbacIcon
                  text: RBAC
                  textKey: menuItem.rbac
            menuItems:
              rbac:
                parent: default.admin
                icon: rbacIcon

  - package: oci://ghcr.io/redhat-developer/rhdh-plugin-export-overlays/backstage-community-plugin-rbac-backend:<tag>
    forceDownload: true
    pullPolicy: Always
    disabled: false

  # Azure-DevOps
  - package: oci://ghcr.io/redhat-developer/rhdh-plugin-export-overlays/backstage-community-plugin-azure-devops:<tag>
    forceDownload: true
    pullPolicy: Always
    disabled: false
    pluginConfig:
      dynamicPlugins:
        frontend:
          backstage-community.plugin-azure-devops:
            mountPoints:
              - mountPoint: entity.page.ci/cards
                importName: EntityAzurePipelinesContent
                config:
                  layout:
                    gridColumn: 1 / -1
                  if:
                    anyOf:
                      - hasAnnotation: dev.azure.com/project
                      - hasAnnotation: dev.azure.com/build-definition

  - package: oci://ghcr.io/redhat-developer/rhdh-plugin-export-overlays/backstage-community-plugin-azure-devops-backend:<tag>
    forceDownload: true
    pullPolicy: Always
    disabled: false

    # Gitlab
  - package: oci://ghcr.io/redhat-developer/rhdh-plugin-export-overlays/immobiliarelabs-backstage-plugin-gitlab:<tag>
    forceDownload: true
    pullPolicy: Always
    disabled: false
    pluginConfig:
      dynamicPlugins:
        frontend:
          immobiliarelabs.backstage-plugin-gitlab:
            mountPoints:
              - mountPoint: entity.page.ci/cards
                importName: EntityGitlabPipelinesTable
                config:
                  layout:
                    gridColumn: 1 / -1
                  if:
                    allOf:
                      - isGitlabAvailable
                      - hasAnnotation: 
                          - gitlab.com/project-slug
              - mountPoint: entity.page.issues/cards
                importName: EntityGitlabIssuesTable
                config:
                  layout:
                    gridColumn: 1 / -1
                  if:
                    allOf:
                      - isGitlabAvailable
              - mountPoint: entity.page.pull-requests/cards
                importName: EntityGitlabMergeRequestsTable
                config:
                  layout:
                    gridColumn: 1 / -1
                  if:
                    allOf:
                      - isGitlabAvailable

  # Gitlab Backend
  - package: oci://ghcr.io/redhat-developer/rhdh-plugin-export-overlays/immobiliarelabs-backstage-plugin-gitlab-backend:<tag>
    forceDownload: true
    pullPolicy: Always
    disabled: false

  # Quay plugin
  - package: oci://<registry>/<username>/<repository>:release-x.y!tssc-plugins-backstage-community-plugin-quay
    forceDownload: true
    pullPolicy: Always
    disabled: false
    pluginConfig:
      dynamicPlugins:
        frontend:
          tssc-plugins.backstage-community-plugin-quay:
            mountPoints:
              - mountPoint: entity.page.ci/cards
                importName: QuayPage
                config:
                  layout:
                    gridColumn: 1 / -1
                  if:
                    allOf:
                      - isQuayAvailable

  - package: oci://<registry>/<username>/<repository>:release-x.y!tssc-plugins-backstage-community-plugin-quay-backend
    forceDownload: true
    pullPolicy: Always
    disabled: false

  # K8s
  - package: oci://ghcr.io/redhat-developer/rhdh-plugin-export-overlays/backstage-plugin-kubernetes:<tag>
    forceDownload: true
    pullPolicy: Always
    disabled: false
    pluginConfig:
      dynamicPlugins:
        frontend:
          backstage.plugin-kubernetes:
            mountPoints:
              - mountPoint: entity.page.kubernetes/cards
                importName: EntityKubernetesContent
                config:
                  layout:
                    gridColumn: 1 / -1
                  if:
                    anyOf:
                      - hasAnnotation: backstage.io/kubernetes-id
                      - hasAnnotation: backstage.io/kubernetes-namespace


  - package: oci://ghcr.io/redhat-developer/rhdh-plugin-export-overlays/backstage-plugin-kubernetes-backend:<tag>
    forceDownload: true
    pullPolicy: Always
    disabled: false

  # TEKTON
  - package: oci://<registry>/<username>/<repository>:release-x.y!tssc-plugins-backstage-community-plugin-tekton
    forceDownload: true
    pullPolicy: Always
    disabled: false
    pluginConfig:
      dynamicPlugins:
        frontend:
          tssc-plugins.backstage-community-plugin-tekton:
            mountPoints:
            - config:
                if:
                  allOf:
                  - isTektonCIAvailable
                layout:
                  gridColumn: 1 / -1
                  gridRowStart: 1
              importName: TektonCI
              mountPoint: entity.page.ci/cards 

  # Jenkins
  - package: oci://ghcr.io/redhat-developer/rhdh-plugin-export-overlays/backstage-community-plugin-jenkins:<tag>
    forceDownload: true
    pullPolicy: Always
    disabled: false
    pluginConfig:
      dynamicPlugins:
        frontend:
          backstage-community.plugin-jenkins:
            mountPoints:
              - mountPoint: entity.page.ci/cards
                importName: EntityJenkinsContent
                config:
                  layout:
                    gridColumn: 1 / -1
                  if:
                    anyOf:
                      - isJenkinsAvailable
              - mountPoint: entity.page.ci/cards
                importName: EntityLatestJenkinsRunCard
                config:
                  layout:
                    gridColumn: 1 / -1
                  if:
                    anyOf:
                      - isJenkinsAvailable

  - package: oci://ghcr.io/redhat-developer/rhdh-plugin-export-overlays/backstage-community-plugin-jenkins-backend:<tag>
    forceDownload: true
    pullPolicy: Always
    disabled: false

  # ArgoCD
  - package: oci://<registry>/<username>/<repository>:release-x.y!tssc-plugins-backstage-community-plugin-argocd
    forceDownload: true
    pullPolicy: Always
    disabled: false
    pluginConfig:
      dynamicPlugins:
        frontend:
          backstage-community.plugin-redhat-argocd:
          #tssc-plugins.backstage-community-plugin-redhat-argocd:
            mountPoints:
              - mountPoint: entity.page.ci/cards
                importName: ArgocdDeploymentLifecycle
                config:
                  layout:
                    gridColumn: 1 / -1
                  if:
                    allOf:
                      - isArgocdConfigured

              - mountPoint: entity.page.ci/cards
                importName: ArgocdDeploymentSummary
                config:
                  layout:
                    gridColumn: 1 / -1
                  if:
                    allOf:
                      - isArgocdConfigured

  # ArgoCD Backend
  - package: oci://<registry>/<username>/<repository>:release-x.y!tssc-plugins-backstage-community-plugin-argocd-backend
    forceDownload: true
    pullPolicy: Always
    disabled: false

  # MSSV
  - package: oci://<registry>/<username>/<repository>:release-x.y!tssc-plugins-backstage-community-plugin-multi-source-security-viewer
    forceDownload: true
    pullPolicy: Always
    disabled: false
    pluginConfig:
      dynamicPlugins:
        frontend:
          tssc-plugins.backstage-community-plugin-multi-source-security-viewer:
            mountPoints:
              - config:
                  layout:
                    gridColumn: 1 / -1
                    gridRowStart: 2
                  if:
                    allOf:
                      - isMultiCIAvailable
                importName: EntityMultiCIPipelinesContent
                mountPoint: entity.page.ci/cards


  # MSSV Backend (For RBAC)
  - package: oci://<registry>/<username>/<repository>:release-x.y!tssc-plugins-backstage-community-plugin-multi-source-security-viewer-backend
    forceDownload: true
    pullPolicy: Always
    disabled: false
